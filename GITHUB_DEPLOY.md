# Подробная инструкция: Загрузка проекта на Netlify через GitHub

## Шаг 1: Создание аккаунта на GitHub (если его нет)

1. Перейдите на https://github.com
2. Нажмите "Sign up" (Зарегистрироваться)
3. Заполните форму:
   - Username (имя пользователя)
   - Email (электронная почта)
   - Password (пароль)
4. Подтвердите email
5. Выберите бесплатный план (Free)

---

## Шаг 2: Создание нового репозитория на GitHub

1. **Войдите в GitHub**
   - Откройте https://github.com
   - Войдите в свой аккаунт

2. **Создайте новый репозиторий**
   - Нажмите на "+" в правом верхнем углу
   - Выберите "New repository"

3. **Заполните форму создания репозитория:**
   - **Repository name**: `bunker-game` (или любое другое название)
   - **Description** (описание): "Bunker Game - онлайн игра" (опционально)
   - **Visibility**: выберите "Public" (публичный) или "Private" (приватный)
     - ⚠️ **Важно**: Для бесплатного Netlify нужен Public репозиторий
   - **НЕ ставьте галочки** на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - Нажмите зеленую кнопку **"Create repository"**

4. **Скопируйте URL репозитория**
   - После создания вы увидите страницу с инструкциями
   - Найдите URL вида: `https://github.com/ВАШ_ЛОГИН/bunker-game.git`
   - Скопируйте его (он понадобится позже)

---

## Шаг 3: Установка Git (если не установлен)

### Проверка, установлен ли Git:

1. Откройте **PowerShell** или **Командную строку** (cmd)
2. Введите команду:
   ```bash
   git --version
   ```
3. Если увидите версию (например, `git version 2.40.0`), значит Git установлен ✅
4. Если ошибка "команда не найдена", нужно установить Git

### Установка Git (если не установлен):

1. Перейдите на https://git-scm.com/download/win
2. Скачайте установщик для Windows
3. Запустите установщик
4. Во время установки:
   - Оставьте все настройки по умолчанию
   - Нажмите "Next" на всех шагах
   - В конце нажмите "Install"
5. После установки перезапустите PowerShell/Командную строку

---

## Шаг 4: Загрузка файлов проекта в GitHub

### Вариант A: Через веб-интерфейс GitHub (Самый простой)

1. **Откройте созданный репозиторий на GitHub**
   - Перейдите на страницу вашего репозитория
   - URL будет: `https://github.com/ВАШ_ЛОГИН/bunker-game`

2. **Нажмите "uploading an existing file"**
   - На странице репозитория вы увидите кнопку "uploading an existing file"
   - Или просто перетащите файлы в окно браузера

3. **Загрузите все файлы проекта**
   - Откройте папку проекта: `C:\Users\Пётр Дулов\Desktop\Новая папка (3)`
   - Выберите ВСЕ файлы:
     - ✅ `index.html`
     - ✅ `script.js`
     - ✅ `style.css`
     - ✅ `netlify.toml`
     - ✅ `users.json`
     - ✅ `bg.jpg`
     - ✅ `bg2.jpg`
     - ✅ `bgc.jpg`
     - ✅ `README.md` (если есть)
   - Перетащите их в окно GitHub или нажмите "choose your files"

4. **Создайте коммит**
   - Внизу страницы в поле "Commit changes" введите: `Initial commit`
   - Нажмите зеленую кнопку **"Commit changes"**

5. **Готово!** Все файлы теперь в GitHub ✅

---

### Вариант B: Через командную строку (Git)

1. **Откройте PowerShell или Командную строку**

2. **Перейдите в папку проекта**
   ```bash
   cd "C:\Users\Пётр Дулов\Desktop\Новая папка (3)"
   ```

3. **Инициализируйте Git репозиторий**
   ```bash
   git init
   ```
   - Вы увидите: `Initialized empty Git repository in ...`

4. **Добавьте все файлы**
   ```bash
   git add .
   ```
   - Точка означает "все файлы в текущей папке"

5. **Создайте первый коммит**
   ```bash
   git commit -m "Initial commit"
   ```
   - Если видите ошибку про имя/email, выполните:
   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "ваш@email.com"
   ```
   - Затем повторите `git commit -m "Initial commit"`

6. **Переименуйте ветку в main**
   ```bash
   git branch -M main
   ```

7. **Подключите удаленный репозиторий**
   ```bash
   git remote add origin https://github.com/ВАШ_ЛОГИН/bunker-game.git
   ```
   - ⚠️ Замените `ВАШ_ЛОГИН` на ваш логин GitHub
   - ⚠️ Замените `bunker-game` на название вашего репозитория

8. **Загрузите файлы на GitHub**
   ```bash
   git push -u origin main
   ```
   - Вас попросят ввести логин и пароль GitHub
   - Для пароля используйте **Personal Access Token** (см. ниже)

9. **Готово!** Файлы загружены на GitHub ✅

---

### ⚠️ Важно: Personal Access Token для Git

GitHub больше не принимает обычные пароли. Нужен токен:

1. **Создайте токен:**
   - GitHub → Settings (настройки профиля)
   - В левом меню: "Developer settings"
   - "Personal access tokens" → "Tokens (classic)"
   - "Generate new token" → "Generate new token (classic)"
   - Название: `netlify-deploy`
   - Выберите срок действия (например, 90 дней)
   - Отметьте галочку: `repo` (полный доступ к репозиториям)
   - Нажмите "Generate token"
   - **Скопируйте токен** (он показывается только один раз!)

2. **Используйте токен вместо пароля:**
   - При команде `git push` введите:
     - Username: ваш логин GitHub
     - Password: вставьте скопированный токен

---

## Шаг 5: Подключение репозитория к Netlify

1. **Откройте Netlify**
   - Перейдите на https://app.netlify.com
   - Войдите в аккаунт (или зарегистрируйтесь)

2. **Создайте новый сайт**
   - Нажмите "Add new site"
   - Выберите "Import an existing project"

3. **Подключите GitHub**
   - Нажмите "Deploy with GitHub"
   - Вас перенаправит на авторизацию GitHub
   - Нажмите "Authorize Netlify"
   - Подтвердите доступ

4. **Выберите репозиторий**
   - В списке найдите `bunker-game` (или название вашего репозитория)
   - Нажмите на него

5. **Настройте деплой**
   - **Build command**: оставьте пустым (или введите `echo "No build needed"`)
   - **Publish directory**: введите точку `.` (это означает корень проекта)
   - Нажмите "Deploy site"

6. **Дождитесь деплоя**
   - Netlify начнет загружать файлы
   - Процесс займет 1-2 минуты
   - Вы увидите прогресс в реальном времени

7. **Готово!**
   - После завершения вы получите ссылку вида: `https://random-name-123.netlify.app`
   - Нажмите на ссылку, чтобы открыть сайт

---

## Шаг 6: Автоматические обновления (Бонус)

Теперь при каждом обновлении кода на GitHub сайт будет обновляться автоматически!

### Как обновить сайт:

**Вариант A: Через веб-интерфейс GitHub**
1. Откройте файл на GitHub
2. Нажмите на иконку карандаша (Edit)
3. Внесите изменения
4. Прокрутите вниз, введите сообщение коммита
5. Нажмите "Commit changes"
6. Netlify автоматически обновит сайт через 1-2 минуты!

**Вариант B: Через командную строку**
```bash
# Перейдите в папку проекта
cd "C:\Users\Пётр Дулов\Desktop\Новая папка (3)"

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

---

## Проверка работы

1. **Откройте сайт на Netlify**
   - Перейдите по ссылке, которую дал Netlify
   - Проверьте, что все работает

2. **Проверьте консоль браузера**
   - Нажмите F12
   - Перейдите на вкладку "Console"
   - Убедитесь, что нет ошибок

3. **Проверьте файлы**
   - Убедитесь, что все изображения загружаются
   - Проверьте, что стили применяются
   - Проверьте, что JavaScript работает

---

## Решение проблем

### Проблема: "Repository not found"
- Убедитесь, что репозиторий публичный (Public)
- Проверьте, что вы авторизовали Netlify в GitHub

### Проблема: "Build failed"
- Проверьте, что в "Publish directory" указана точка `.`
- Убедитесь, что файл `index.html` находится в корне проекта

### Проблема: Сайт не обновляется
- В Netlify Dashboard → Deploys → нажмите "Trigger deploy" → "Clear cache and deploy site"
- Проверьте, что изменения действительно загружены в GitHub

### Проблема: Ошибки при git push
- Убедитесь, что используете Personal Access Token, а не пароль
- Проверьте, что репозиторий создан и URL правильный

---

## Полезные команды Git

```bash
# Проверить статус файлов
git status

# Посмотреть историю коммитов
git log

# Отменить последний коммит (если еще не загружен)
git reset --soft HEAD~1

# Обновить файлы с GitHub
git pull
```

---

## Готово! 🎉

Теперь ваш проект:
- ✅ Хранится на GitHub
- ✅ Автоматически деплоится на Netlify
- ✅ Обновляется при каждом изменении кода
- ✅ Доступен по постоянной ссылке

**Следующие шаги:**
- Настройте кастомный домен (опционально)
- Добавьте описание проекта в README.md
- Пригласите друзей поиграть! 🎮

