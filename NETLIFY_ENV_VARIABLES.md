# Правильная настройка переменных окружения в Netlify

## ⚠️ ОШИБКА: "Variable names can only consist of alphanumeric characters and underscores"

Эта ошибка появляется, если в **названии** переменной есть недопустимые символы.

## ✅ Правильная настройка:

### Шаг 1: Откройте настройки переменных окружения

1. В Netlify Dashboard откройте ваш сайт
2. Перейдите в **Site settings** (Настройки сайта)
3. В левом меню выберите **Environment variables** (Переменные окружения)

### Шаг 2: Добавьте переменные правильно

**ВАЖНО:** 
- **Key** (ключ) = название переменной (только буквы, цифры и подчеркивания)
- **Value** (значение) = ваши данные из Supabase

#### Переменная 1: SUPABASE_URL

1. Нажмите **"Add a variable"** (Добавить переменную)
2. **Key (ключ)**: `SUPABASE_URL` 
   - ⚠️ Только заглавные буквы и подчеркивания!
   - ❌ НЕ пишите: `SUPABASE URL` (пробел недопустим)
   - ❌ НЕ пишите: `Supabase-URL` (дефис недопустим)
   - ✅ Правильно: `SUPABASE_URL`
3. **Value (значение)**: вставьте ваш Project URL из Supabase
   - Например: `https://xxxxx.supabase.co`
   - ⚠️ Включайте `https://` в начале!
4. Нажмите **"Save"**

#### Переменная 2: SUPABASE_ANON_KEY

1. Нажмите **"Add a variable"** еще раз
2. **Key**: `SUPABASE_ANON_KEY`
3. **Value**: вставьте ваш **anon public key** из Supabase
   - Это длинный ключ вида: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
4. Нажмите **"Save"**

#### Переменная 3: SUPABASE_SERVICE_KEY

1. Нажмите **"Add a variable"** еще раз
2. **Key**: `SUPABASE_SERVICE_KEY`
3. **Value**: вставьте ваш **service_role key** из Supabase
   - Это длинный ключ вида: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
   - ⚠️ Это секретный ключ! Не показывайте никому!
4. Нажмите **"Save"**

### Шаг 3: Передеплойте сайт

**ОБЯЗАТЕЛЬНО!** После добавления переменных нужно передеплоить сайт:

1. Перейдите в **Deploys** (Деплои)
2. Нажмите **"Trigger deploy"** → **"Clear cache and deploy site"**
3. Или сделайте новый commit и push в GitHub (если используете Git)

## ✅ Проверка:

После передеплоя:

1. Попробуйте зарегистрировать пользователя
2. Проверьте в Supabase: Table Editor → users
3. Должен появиться новый пользователь

## ❌ Частые ошибки:

### Ошибка 1: Пробелы в названии
- ❌ `SUPABASE URL` 
- ✅ `SUPABASE_URL`

### Ошибка 2: Дефисы в названии
- ❌ `SUPABASE-URL`
- ✅ `SUPABASE_URL`

### Ошибка 3: Маленькие буквы (работает, но лучше заглавные)
- ⚠️ `supabase_url` (работает, но не рекомендуется)
- ✅ `SUPABASE_URL` (рекомендуется)

### Ошибка 4: URL без https://
- ❌ `xxxxx.supabase.co`
- ✅ `https://xxxxx.supabase.co`

### Ошибка 5: Не передеплоили после добавления переменных
- ⚠️ Переменные не будут доступны, пока не передеплоите!

## 📋 Чек-лист:

- [ ] Добавлена переменная `SUPABASE_URL` с правильным названием
- [ ] Добавлена переменная `SUPABASE_ANON_KEY` с правильным названием
- [ ] Добавлена переменная `SUPABASE_SERVICE_KEY` с правильным названием
- [ ] Все значения скопированы правильно (с https:// для URL)
- [ ] Сайт передеплоен после добавления переменных
- [ ] Таблица `users` создана в Supabase
- [ ] Политики RLS настроены в Supabase

## 🔍 Проверка работы:

1. Откройте Netlify → Functions → getUsers → View logs
2. Попробуйте зарегистрировать пользователя
3. Проверьте логи - не должно быть ошибок "Supabase не настроен"

Если видите ошибку "Supabase credentials not configured" - значит переменные не загружены или названы неправильно.

