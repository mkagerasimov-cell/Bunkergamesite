# Настройка Supabase для хранения пользователей

## Шаг 1: Создание проекта в Supabase

1. **Перейдите на Supabase**
   - Откройте https://supabase.com
   - Нажмите "Start your project" или "Sign in"

2. **Создайте аккаунт** (если его нет)
   - Используйте GitHub, Google или email
   - Это бесплатно

3. **Создайте новый проект**
   - Нажмите "New Project"
   - **Organization**: выберите или создайте организацию
   - **Name**: `bunker-game` (или любое другое название)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион (например, `West US` или `Central EU`)
   - Нажмите "Create new project"
   - ⏳ Подождите 2-3 минуты, пока проект создается

## Шаг 2: Получение API ключей

1. **Откройте настройки проекта**
   - В левом меню нажмите на иконку ⚙️ (Settings)
   - Выберите "API"

2. **Скопируйте ключи:**
   - **Project URL**: `https://xxxxx.supabase.co` (скопируйте)
   - **anon public key**: `eyJhbGc...` (длинный ключ, скопируйте)
   - **service_role key**: `eyJhbGc...` (НЕ показывайте никому!)

3. **Сохраните эти ключи** - они понадобятся для настройки

## Шаг 3: Создание таблицы в базе данных

1. **Откройте SQL Editor**
   - В левом меню нажмите на иконку SQL (или "SQL Editor")

2. **Создайте таблицу пользователей**
   - Нажмите "New query"
   - Вставьте следующий SQL код:

```sql
-- Создание таблицы пользователей
CREATE TABLE IF NOT EXISTS users (
  id BIGSERIAL PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  email TEXT,
  registered_at TIMESTAMPTZ DEFAULT NOW(),
  is_admin BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Создание индекса для быстрого поиска по username
CREATE INDEX IF NOT EXISTS idx_users_username ON users(username);

-- Включение Row Level Security (RLS)
ALTER TABLE users ENABLE ROW LEVEL SECURITY;

-- Политика: разрешить всем читать пользователей (для публичного API)
CREATE POLICY "Allow public read access" ON users
  FOR SELECT
  USING (true);

-- Политика: разрешить всем создавать пользователей (для регистрации)
CREATE POLICY "Allow public insert" ON users
  FOR INSERT
  WITH CHECK (true);

-- Политика: разрешить всем обновлять пользователей (для админки через service_role)
-- Это будет работать через service_role key, который обходит RLS
```

3. **Выполните запрос**
   - Нажмите "Run" или Ctrl+Enter
   - Должно появиться сообщение об успехе

4. **Проверьте таблицу**
   - В левом меню: "Table Editor"
   - Должна появиться таблица `users`

## Шаг 4: Настройка переменных окружения в Netlify

1. **В Netlify Dashboard**
   - Откройте ваш сайт
   - Перейдите в "Site settings" → "Environment variables"

2. **Добавьте переменные:**
   - Нажмите "Add a variable"
   - Добавьте следующие переменные:

```
SUPABASE_URL = https://xxxxx.supabase.co (ваш Project URL)
SUPABASE_ANON_KEY = eyJhbGc... (ваш anon public key)
SUPABASE_SERVICE_KEY = eyJhbGc... (ваш service_role key)
```

3. **Сохраните переменные**
   - Нажмите "Save"
   - ⚠️ **Важно**: После добавления переменных нужно передеплоить сайт!

## Шаг 5: Обновление кода

Код уже обновлен для работы с Supabase! Просто загрузите обновленные файлы на Netlify.

## Шаг 6: Тестирование

1. **Зарегистрируйте пользователя** на сайте
2. **Проверьте в Supabase:**
   - Table Editor → users
   - Должен появиться новый пользователь
3. **Откройте админку** - пользователь должен отображаться

## Готово! 🎉

Теперь все пользователи хранятся в Supabase и синхронизируются между всеми устройствами в реальном времени!

---

## Полезные ссылки:

- Supabase Dashboard: https://app.supabase.com
- Документация Supabase: https://supabase.com/docs
- SQL Editor: https://app.supabase.com/project/_/sql

## Безопасность:

⚠️ **Важно:**
- `SUPABASE_SERVICE_KEY` - НИКОГДА не показывайте в клиентском коде!
- Используйте его только в Netlify Functions (на сервере)
- `SUPABASE_ANON_KEY` можно использовать в клиентском коде (он ограничен RLS политиками)

